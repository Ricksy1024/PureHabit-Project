# Code Review Report: Habit Tracker Backend Core

**Reviewer**: GitHub Copilot (Staff Engineer Perspective)
**Date**: 2026-04-09
**Feature**: specs/001-habit-tracker-backend/spec.md
**Branch**: 001-habit-tracker-backend
**Verdict**: ⚠️ APPROVED WITH CONDITIONS

---

## Executive Summary

The implementation is functionally complete against the feature specification and prior blocker findings are resolved (TOTP-only callable guard, transactional sync merge, habit ownership validation). Test evidence is strong (9/9 suites passing, 57/57 tests passing) with solid overall coverage (83.53% statements). Remaining issues are warning-level and concentrated in security hardening of TOTP verification (no attempt throttling) and scalability behavior in streak recomputation and reminder scheduling under sustained load.

---

## Review Findings

| ID | Severity | File | Line(s) | Category | Finding | Recommendation |
|----|----------|------|---------|----------|---------|----------------|
| R001 | 🟡 Warning | functions/src/handlers/api.js | 112-168 | Security | `verifyTOTPHandler` validates token format and checks the TOTP code, but does not implement retry throttling, lockout windows, or attempt counters. This allows high-volume online guessing attempts against a 6-digit factor if credentials are already compromised. | Add per-user and per-IP attempt limits (short-window throttling + temporary lockout) and emit audit logs for repeated failures. |
| R002 | 🟡 Warning | functions/src/handlers/triggers.js | 243-263 | Performance, Scalability | `onHabitLogWriteHandler` re-queries a large date window and recomputes streaks on every log write. With default window = 730 days, this remains read-heavy and scales poorly for high-activity users/habits. | Move to incremental streak updates keyed to affected logical day, keeping full-window recomputation for repair/backfill only. |
| R003 | 🟡 Warning | functions/src/handlers/triggers.js | 326-389, 397-399 | Reliability, Performance | Reminder scheduler stops early when `REMINDER_MAX_HABITS_PER_RUN` is reached and only logs warnings. Without durable cursor/checkpoint progression, sustained backlog can repeatedly defer tail habits. | Persist a cursor or shard assignment checkpoint between runs so the scheduler advances deterministically across the full habit set. |
| R004 | 🟢 Suggestion | functions/tests/handlers/test_triggers.js | 147-374 | Test Quality | Trigger/scheduler tests validate correctness and bounds, but do not exercise high-cardinality/backlog progression behavior (multi-run fairness and catch-up). | Add stress-style integration tests to assert eventual coverage of all due habits under capped runs and partitioned scheduling. |

**Categories**: Correctness, Security, Performance, Spec Compliance, Test Quality, Architecture

---

## Spec Coverage Matrix

| Requirement | Status | Implementation Notes |
|-------------|--------|---------------------|
| FR-001: Process offline sync payloads with past timestamps reliably | ✅ Implemented | `syncHabitLogsHandler` accepts historical payloads and computes logical day with timezone-aware logic. |
| FR-002: Merge conflicts using Logical OR (`true` overrides `false`) | ✅ Implemented | `computeMerge` applies OR semantics; sync writes are wrapped in Firestore transactions. |
| FR-003: Determine current day using user timezone | ✅ Implemented | Sync and trigger paths resolve timezone from user profile with normalized fallback behavior. |
| FR-004: Suppress reminders if already completed that day | ✅ Implemented | Scheduler computes logical day and suppresses sends when completion already exists. |
| FR-005: Preserve longest streak when rules change | ✅ Implemented | Streak calculator preserves `previousLongest` while recomputing current streak. |
| FR-006: Securely pair and mandate TOTP during auth | ✅ Implemented | Secret encryption, TOTP setup/verify flow, and `totpVerified` callable auth guard are in place. |

**Coverage**: 6/6 requirements implemented (100%).

---

## Test Coverage Assessment

| Area | Tests Exist? | Coverage | Gaps |
|------|-------------|----------|------|
| Auth core + middleware (`core/auth.js`, `handlers/authMiddleware.js`) | ✅ | Strong functional coverage | No explicit throttling/lockout behavior tested (not yet implemented) |
| Sync core + handler (`core/sync.js`, `handlers/api.js`) | ✅ | Strong, includes concurrency and ownership checks | Large-batch throughput characteristics not performance-tested |
| Streak trigger path (`handlers/triggers.js`) | ✅ | Functional correctness covered | Full-window recompute cost under high event volume not load-tested |
| Reminder scheduler (`handlers/triggers.js`) | ✅ | Functional and cap/partition behavior covered | Multi-run backlog fairness/eventual catch-up not asserted |

---

## Metrics Summary

| Metric | Value |
|--------|-------|
| Files reviewed | 37 |
| 🔴 Blockers | 0 |
| 🟡 Warnings | 3 |
| 🟢 Suggestions | 1 |
| Spec coverage | 100% (6/6 FRs) |
| Test status | 9/9 suites passing, 57/57 tests passing |
| Jest coverage | 83.53% statements, 65.99% branches, 87.09% functions, 83.73% lines |

---

## Recommended Actions

### Must Fix (Blockers)
1. None.

### Should Fix (Warnings)
1. Implement TOTP verification attempt throttling/lockout controls in `verifyTOTPHandler`.
2. Replace per-write full-window streak recomputation with incremental updates and bounded fallback recalculation.
3. Add durable scheduler progression (cursor/shard checkpointing) to prevent chronic tail-habit deferral when caps are reached.

### Nice to Fix (Suggestions)
1. Add load/backlog progression tests for trigger and scheduler behavior under high-cardinality datasets.

---

*Generated by `/speckit.staff-review.run` — Staff-level code review for spec-driven development.*
